## 만드는 것

- 충남대학교 생명정보학과 3학년 학생이 채용공고를 찾는 화면 하나
- 우선 지원 대상: 바이오·헬스케어 데이터 분석 인턴
- 지원 지역: 대전·세종·충청권
- 그 밖의 사용자나 사용 목적: (모름)

## 화면

- 공고의 직무명과 직무 분야
- 채용 형태
- 근무지
- 재학생 지원 가능 여부
- 공고 카드에서 `재학생 지원 가능` 여부를 크게 표시
- 공고를 열기 전 지원 자격을 가장 먼저 판단할 수 있게 표시
- 관심 있는 공고에서 같은 채용 형태의 비슷한 공고를 확인하는 기능
- 지역·채용 형태·지원 자격을 다시 선택하는 조건 변경 기능
- 학력·학년·전공 조건의 상세 표시 방식: (모름)
- 회사 정보의 표시 방식: (모름)
- 마감일과 채용 절차의 표시 방식: (모름)
- 색상·글꼴·화면 분위기: (모름)

## 추천 규칙

- 공고 데이터에 재학생 지원 가능이라고 명시된 공고만 보여준다.
- 채용 형태가 인턴이고 근무지가 대전·세종·충청권인 공고만 보여준다.
- 그중 직무 분야가 바이오·헬스케어 데이터 분석인 공고를 가장 위로 올린다.
- 비슷한 공고는 채용 형태가 같은 공고를 기준으로 보여준다.
- 위 조건이 같은 공고 사이의 정렬 순서: (모름)

## 평가 기준

- `바이오·헬스케어 데이터 분석`을 누르면 해당 분야 공고만 보인다.
- `인턴·충청권`을 누르면 대전·세종·충청권의 인턴 공고만 보인다.
- `재학생 지원 가능`을 누르면 재학생이 지원할 수 있는 공고만 보인다.
- `비슷한 공고`를 누르면 같은 채용 형태의 공고가 보인다.
- `조건 변경`을 누르면 지역·채용 형태·지원 자격 선택 항목이 보인다.
- 조건을 다시 선택하면 변경된 조건에 맞는 공고 목록이 보인다.
- 공고 상세 화면에서 무엇을 누르면 무엇이 보이는지: (모름)

## 이번에 안 만드는 것

- 신입 공고를 지원 대상으로 추천하는 기능
- 프로젝트 폴더의 API 연동
- 그 밖의 제외 범위: (모름)
